# Local development defaults to SQLite when DATABASE_URL is omitted.
# Production: use a long, random manager key and PostgreSQL.
MANAGER_KEY=replace-with-a-long-random-secret
DATABASE_URL=sqlite:///./data/profiles.db
